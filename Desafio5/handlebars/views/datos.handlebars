<h1 style="color:navy;">Ingrese datos</h1>
<br>

<form action="/productos" method="post">

    <div class="form-group">
        <label for="nombre"><b>Nombre</b></label>
        <input id="nombre" class="form-control" type="text" name="nombre">
    </div>

    <div class="form-group">
        <label for="precio"><b>Precio</b></label>
        <input id="precio" class="form-control" type="number" name="precio">
    </div>

    <div class="form-group">
        <label for="imagen"><b>URL Imagen</b></label>
        <input id="imagen" class="form-control" type="text" name="imagen">
    </div>

    <button class="btn btn-success mt-3 mb-5">Enviar datos</button>
</form>

<h2 style="color:brown;">Historial</h2>

{{!-- {{#if (productos.length > 0) }}  --}}

{{#if productos }} 
    <div class="table-responsive">
        <table class="table table-dark">
            <tr style="color: yellow;"> <th>Nombre</th> <th>Precio</th> <th>Imagen</th> </tr>
            {{!-- for (const producto of productos) { --}}
            {{#each productos}}
            <tr>
                <td>{{nombre}}</td>
                <td>{{precio}}</td>
                <td><img style="width:80px;height:80px"alt="no image"src="{{imagen}}"></td>
            </tr>
            {{/each}}
            {{!-- }; --}}
        </table>
    </div>
 {{else}} 
        <h3 class="alert alert-danger">No se encontraron datos</h3>
{{/if}}